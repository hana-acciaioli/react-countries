import React from 'react';

export default function Controller(option) {
  return (
    <controller>
      <select>
        <option value={option}>option</option>
      </select>
    </controller>
  );
}
